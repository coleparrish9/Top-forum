<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css"
    />
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.5.0/font/bootstrap-icons.css"
    />
    <link rel="stylesheet" href="/Top-forum/pub/css/style.css">
    <title>Coles Crawler Forum</title>
</head>
<body>

     <header class="navBar">
      <nav>
        {{#if logged_in}}
          <a href="/">Home</a>
          <a href="/dashboard">Dashboard</a>
          <button class="no-button" id="logout">Log-out</button>
        {{else}}
          <a href="/">Home</a>
          <a href="/dashboard">Dashboard</a>
          <a href="/login">Log-in</a>
        {{/if}}
      </nav>
    </header>

    <h1 class="text-center">Coles Crawler Forum</h1>

    <main>
      {{{body}}}
    </main>

    {{#if logged_in}}
      <script src="/Top-forum/pub/js/logout.js"></script>
    {{/if}}

    
</body>
</html>